---
- name: Enroll and install EST certificates on ClearPass
  hosts: publishers
  connection: local
  gather_facts: false
  roles:
    - role: clearpass_cert
      vars:
        clearpass_cert_action: enroll_est
