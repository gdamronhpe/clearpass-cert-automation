---
- name: Preflight ClearPass API connectivity and auth checks
  hosts: publishers
  connection: local
  gather_facts: false
  roles:
    - role: clearpass_cert
      vars:
        clearpass_cert_action: preflight
